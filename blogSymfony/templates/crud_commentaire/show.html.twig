{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commentaire.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crud_commentaire_index') }}">back to list</a>

    <a href="{{ path('app_crud_commentaire_edit', {'id': commentaire.id}) }}">edit</a>

    {{ include('crud_commentaire/_delete_form.html.twig') }}
{% endblock %}
